import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#ff6b00',
            light: '#fff8ec',
            dark: '#461704'
        },
        secondary: {
            main: '#461704'
        }
    }
})

export {theme}
